{
  "entities": {
    "Project": {
      "title": "Project",
      "description": "A portfolio project item",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "url": { "type": "string" },
        "image": { "type": "string" },
        "category": { "type": "string" },
        "alt": { "type": "string" },
        "order": { "type": "number" }
      },
      "required": ["name", "url", "image", "category"]
    },
    "SiteContent": {
      "title": "Site Content",
      "description": "Global or section-specific site content",
      "type": "object",
      "properties": {
        "heroTitle": { "type": "string" },
        "heroSubtitle": { "type": "string" },
        "heroImages": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    }
  },
  "firestore": {
    "/projects/{projectId}": {
      "schema": "Project",
      "description": "Collection of portfolio projects"
    },
    "/siteContent/{docId}": {
      "schema": "SiteContent",
      "description": "Document for general site content"
    },
    "/admins/{userId}": {
      "schema": {
        "type": "object",
        "properties": {
          "email": { "type": "string" }
        }
      },
      "description": "List of admin users"
    }
  }
}
